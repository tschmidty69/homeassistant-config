openGarageDoor:  
  speech:
    type: plain
    text: >-
      {%- if is_state("sensor.ecolink_garage_door_tilt_sensor_sensor", "off") -%}
        OK, opening the garage door.
      {%- else -%}
        The garage door is already open.
      {%- endif %}
  action:
    - service: script.turn_on
      entity_id: script.garage_door_open

closeGarageDoor:
  speech:
    type: plain
    text: >-
      {%- if is_state("sensor.ecolink_garage_door_tilt_sensor_sensor", "on") -%}
        OK, closing the garage door
      {%- else -%}
        The garage door is already closed.
      {%- endif %}
  action:
    - service: script.garage_door_close

searchWeatherForecast:
  speech:
    type: plain
    text: weather
  action:
    - service: script.current_weather

playPlaylist:
  speech:
    type: plain
    text: music
  action:
    - service: script.playlist_mopidy
      data_template:
        playlist: "{{ playlist_name | default ('christmas') }}"

resumeMusic:
  speech:
    type: plain
    text: music
  action:
    - service: script.resume_mopidy

speakerInterrupt:
  speech:
    type: plain
    text: music
  action:
    - service: script.pause_mopidy

volumeUp:
  speech:
    type: plain
    text: music
  action:
    - service: script.volume_up_mopidy

volumeDown:
  speech:
    type: plain
    text: music
  action:
    - service: script.volume_down_mopidy

nextSong:
  speech:
    type: plain
    text: music
  action:
    - service: script.next_track_mopidy

previousSong:
  speech:
    type: plain
    text: music
  action:
    - service: script.previous_track_mopidy

