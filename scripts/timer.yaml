set_timer:
  sequence:
    - event: JARVIS_SET_TIMER
      event_data_template:
        name: '{{ name  }}'
        hours: '{{ hours }}'
        minutes: '{{ minutes }}'
        seconds: '{{ seconds }}'
